{% extends 'base.html.twig' %}

{% block title %}{{ lesson.id ? 'Editar Aula' : 'Nova Aula'}}{% endblock %}

{% block body %}
    <h1>{{ lesson.id ? 'Atualizar Aula' : 'Cadastrar Aula'}}</h1>

    {{ form(form) }}

    <a href="{{ path('app_course_index') }}" class="btn btn-warning mt-2">Voltar ao Catálogo</a>
{% endblock %}
